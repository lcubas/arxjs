/** Injection token for ArxModuleOptions inside the DI container. */
export const ARX_MODULE_OPTIONS = Symbol('ARX_MODULE_OPTIONS');

/** Metadata key set by @RequirePermissions(). */
export const ARX_PERMISSIONS_KEY = 'arx:permissions';

/** Metadata key set by @RequireRole(). */
export const ARX_ROLES_KEY = 'arx:roles';
