/**
 * MySQL schema reference for @arx/drizzle
 *
 * Import this schema and pass it to DrizzleAdapter when your project
 * uses a MySQL database via Drizzle ORM.
 *
 * @example
 * import { schema } from '@arx/drizzle/schema/mysql'
 * import { DrizzleAdapter } from '@arx/drizzle'
 * const adapter = new DrizzleAdapter(db, { schema })
 */

// Placeholder — schema tables implemented in Phase 2.6
export const schema = {} as const;
